# 构建顺序(bottom-up)
1 core/config 2 core/logging 3 core/exceptions → 4 schemas/mod →
5 services/rules 6 services/moderate(依赖 rules+config) 7 services/jobs →
8 routers/health 9 routers/moderate(依赖 moderate+jobs) → 10 main 11 run → 12 tests / Dockerfile
> 依赖只从上往下;规则后端默认,测试离线。
