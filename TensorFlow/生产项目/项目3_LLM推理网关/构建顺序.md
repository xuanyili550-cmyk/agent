# 构建顺序(bottom-up)
1 core/config 2 core/logging 3 core/exceptions → 4 schemas/chat →
5 services/cache 6 services/ratelimit 7 services/llm(多后端+降级) →
8 routers/health 9 routers/chat(限流+缓存+调 llm) → 10 main 11 run → 12 tests/Dockerfile
> 外部后端抽象成 llm.chat,默认 stub 让测试离线;真实后端失败自动降级。
