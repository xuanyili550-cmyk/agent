# 生产工程差距对照 · 求职清单 vs 实战练习覆盖

> 对照「推理/RAG/Agent 工程师」求职清单，实事求是标出**真跑代码 ✅ / 只到架构说明 🟡 / 原本缺失❌(已补)**。
> 面试用法：✅ 的重点讲（能展开代码细节）；🟡 的讲清「原理 + 为什么本机不跑（要 GPU/集群）」；
> 已补的 6 块（`../实战练习/生产工程补齐/`）作为「我意识到短板并补齐」的加分项。

## 一、模型基础服务
| 能力点 | 覆盖 | 在哪 |
|---|---|---|
| KV Cache 复用 / 连续批处理原理 | ✅ | `面试大纲` 七、`生产架构/生产01`、`综合项目/项目6`（真跑吞吐对比） |
| 吞吐(tokens/s) | ✅ | `项目6`、`生产工程补齐/补1` |
| **首Token延迟 TTFT** | ✅(已补) | `生产工程补齐/补1` |
| 显存峰值 | ✅(已补) | `补1`（torch memory API） |
| vLLM 部署 / 客户端 | 🟡 需GPU | `生产架构/生产01、生产05` |
| **llama.cpp / GGUF 量化** | 🟡(已补) | `生产工程补齐/补6`（llama-cpp-python 真代码，需 .gguf） |
| AWQ / GPTQ 量化优劣 | 🟡→✅对比 | `补6` docstring 三方案对比；真跑需 GPU |
| Docker / K8s 打包 | 🟡 配置参考 | `生产架构/部署_Docker_Compose_K8s` |
| FastAPI + SSE / WebSocket 流式 | ✅ | `生产级项目_NL2SQL`（完整）、`Agent工程化案例2`（WebSocket）、`生产05`（SSE 客户端） |
| 网关 权限/审核/防注入 | ✅审核 / 🟡其余 | 审核=`综合项目/项目4`(真跑)；鉴权/限流/防注入=`生产01`、`MCP案例5`(OAuth/RBAC/对策) |

## 二、技术选型 —— ✅ 基本全覆盖
Prompt优先→微调→不预训练、RAG vs 微调 选型：`面试大纲` 八、`案例3`、`项目3`。
（单点场景有客服/分类/摘要；「合同提取」类信息抽取可用 NER 迁移。）

## 三、RAG / Agent 全链路
| 能力点 | 覆盖 | 在哪 |
|---|---|---|
| **文档解析 PDF/表格/OCR** | 🟡(已补) | `生产工程补齐/补2`（PyMuPDF 抽文本+表格真跑；OCR 需引擎） |
| 分块策略 / Embedding | ✅ | `项目2`、`中文_语义检索RAG` |
| HNSW / IVF 索引取舍 | 🟡 说明 | `生产架构/生产02`（Qdrant） |
| BGE-Reranker 重排 | 🟡 | `Agent进阶3` 有真 `CrossEncoder` 参考，主流程用简单重排 |
| **手写 ReAct 裸循环** | ✅(已补) | `生产工程补齐/补3`（思考→行动→观测 + 兜底） |
| 工具 Schema / 失败兜底 | ✅ | `Agent实战` 系列、`NL2SQL`（SQL 重试兜底） |

## 四、规范与评测
| 能力点 | 覆盖 | 在哪 |
|---|---|---|
| RESTful + 超时/重试 | ✅ | `生产级项目_NL2SQL/services/llm.py`（重试） |
| **Prompt 的 Git 版本管理** | ✅(已补) | `生产工程补齐/补5`（注册/回滚/diff/A-B） |
| 准确率/召回率 | ✅ | `案例1`、`补4` |
| **幻觉率 + LLM-as-judge 自动评测** | ✅(已补) | `生产工程补齐/补4` |
| 全链路埋点/可观测 | 🟡 参考 | `Agent实战/生产_Agent部署`（Langfuse/OpenTelemetry） |

## 五、上线与兜底 —— 🟡 多为架构说明
监控告警/多副本 HPA/Redis 缓存：`生产架构/部署_Docker_Compose_K8s`、`生产01`（配置参考，非真部署）。
降级兜底：转人工=`综合大demo`、SQL 兜底=`NL2SQL`；「向量库超时切关键词」这类具体降级可按 `补3` 的兜底思路扩展。

---

## 一句话面试策略
- **真能展开的（讲代码）**：RAG 检索全链路、分类/NER/情感微调、LoRA/GRPO、连续批处理+吞吐、FastAPI 工程、TTFT/评测/ReAct/Prompt 版本（补1-5）。
- **讲原理+为什么不本机跑**：vLLM/量化/Docker-K8s/监控/多副本——都因为要 GPU/集群，项目给的是架构参考。
- **主动提短板已补**：文档解析、手写 ReAct、评测体系、Prompt 版本、GGUF——体现「知道生产要什么并补齐」。
