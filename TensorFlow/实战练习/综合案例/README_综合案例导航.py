"""
================================================================================
 综合案例导航 · 把「现有所有技术」综合成端到端应用（NLP + RAG + MCP + Agent + LLM）
================================================================================
 前面各目录是【单点/分章】能力；本目录把它们【综合】成完整应用，每个案例都串起多项技术、可跑可自检。
 覆盖的技术：Ch1 情感 / Ch5 语义检索 / Ch6-7 分词NER / Ch11 LoRA / MCP 协议 / Agent 编排 /
            ChromaDB 向量库 / mlx-lm 本地生成。中文任务用中文模型(dianping/cluener/bge-small-zh)。

 —— 六个综合案例 ——
   综合1_企业智能助手.py       意图路由 → RAG检索/情感NER/计算 → mlx-lm 综合回答    (RAG+NLP+计算+LLM)
   综合2_内容风控全链路.py      风险分级(情感) + NER脱敏 + 置信度决策               (NLP+决策，确定性)
   综合3_文档智能处理.py        文档→切块→实体+情感→抽取摘要→ChromaDB→检索问答    (Ch1/5/6/7+库+LLM)
   综合4_MCP能力Agent.py       Agent 用【远程MCP工具(情感/NER)】+【本地检索】       (MCP+Agent+RAG)
   综合5_微调到服务闭环.py      聊天模板→LoRA微调→保存→基座+适配器重载→推理       (Ch11+部署，真训练)
   综合6_全能力客服Agent.py     工单→情感分流+NER路由+RAG检索+mlx生成回复+决策       (集大成)

 —— 本目录还有两个子集(不同侧重) ——
   生产综合案例/   5 个【真实生产应用】(带 Gradio Web + FastAPI/部署)，5 个合起来覆盖全课程功能。
                  见 生产综合案例/README_生产综合案例.py
   全功能案例/     5 个【按领域穷尽全部功能】的 showcase(非应用场景) + 总清单，共 93 项功能自校验。
                  见 全功能案例/README_全功能总清单.py
   三者关系：本层综合1-6=端到端应用整合；生产综合案例=可上线应用；全功能案例=每个功能都跑到、带覆盖清单。

 —— 编排说明(重要) ——
   本机小模型(0.5B)做“LLM 选工具”不可靠(已实测)，故综合案例的【编排/路由用确定性规则】(稳)，
   LLM 只负责“把工具结果组织成自然语言回答”。生产换大模型 + smolagents/function calling
   可让 LLM 主导编排(见 ../Agent实战)。

 跑：python3 综合1_企业智能助手.py   (等)  每个都带自检；含 mlx/LoRA 的稍慢。
================================================================================
"""
CASES = [
    ("综合1_企业智能助手", "意图路由→RAG/情感NER/计算→LLM回答", "Ch5+Ch1+Ch6/7+mlx"),
    ("综合2_内容风控全链路", "风险分级+NER脱敏+置信度决策", "Ch1+Ch6/7"),
    ("综合3_文档智能处理", "切块+实体+情感+抽取摘要+ChromaDB+问答", "Ch1/5/6/7+库+mlx"),
    ("综合4_MCP能力Agent", "MCP远程工具+本地检索+Agent路由", "MCP+Agent+RAG"),
    ("综合5_微调到服务闭环", "聊天模板+LoRA+保存重载+推理", "Ch11+部署"),
    ("综合6_全能力客服Agent", "情感+NER+RAG+LLM生成+决策(集大成)", "全部"),
]

if __name__ == "__main__":
    print(__doc__)
    print(f" {'案例':<24}{'做什么':<40}{'整合'}")
    for name, what, tech in CASES:
        print(f" {name:<24}{what:<40}{tech}")
