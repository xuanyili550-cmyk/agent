"""
================================================================================
 面试高频题库 · NLP / LLM 工程师（市场上问得最多的，附答案 + 指到对应项目）
================================================================================
 用法：python3 面试高频题库.py            # 打印全部
      python3 面试高频题库.py 微调         # 只看某类
 覆盖 Ch1-12 的技术点，按面试常考的主题分类。答案是“能说清楚 + 有深度”的版本。
================================================================================
"""
import sys

QA = {
"一、Transformer / 基础(Ch1-2)": [
 ("pipeline 一行代码内部做了什么？",
  "三步：①分词器把文本转 input_ids/attention_mask ②模型前向输出 logits "
  "③后处理(softmax→概率、argmax→标签)。所以 pipeline = tokenizer + model + 后处理。"),
 ("attention_mask 是干嘛的？不给会怎样？",
  "标记哪些是真实 token(1)、哪些是填充 [PAD](0)，让注意力忽略填充位。不给的话，"
  "补齐的 [PAD] 会参与注意力计算、污染结果——同一句单独跑 vs 放进补齐的 batch 里跑，logits 会不同。"),
 ("logits 和 softmax 的关系？为什么模型只输出 logits？",
  "logits 是最后一层未归一化的分数(任意实数)。softmax 把它变成 [0,1] 且和为 1 的概率。"
  "模型输出 logits 是因为训练用的交叉熵损失内部自带 softmax(log_softmax)，数值更稳；推理时才手动 softmax。"),
 ("token_type_ids 什么时候用？",
  "句子对任务(问答/句子关系)，用 0/1 区分句 A / 句 B。单句时全 0。"),
],
"二、微调 / 训练工程(Ch2,7)": [
 ("动态填充(DataCollatorWithPadding)比固定 max_length 好在哪？",
  "动态填充只补到‘当前 batch 最长’，而不是全局 max_length。短样本多时能省大量无效计算和显存。"),
 ("显存不够怎么训大模型？(阶梯)",
  "梯度累积(小 batch 多步模拟大 batch)→ 梯度检查点(用时间换显存，重算激活)→ 混合精度(bf16/fp16)→ "
  "LoRA(只训<1%参数)→ QLoRA(基座4bit量化)→ DeepSpeed ZeRO(把权重/梯度/优化器状态切到多卡)+ offload。"),
 ("学习率 warmup 为什么需要？",
  "训练初期参数还很乱，一上来就大学习率容易把模型带崩；先用小学习率热身几步，再升到目标值，更稳。"),
 ("怎么判断过拟合？怎么解决？",
  "训练 loss 降但验证 loss 回升、训练/验证准确率差距拉大。解决：正则(weight_decay/dropout)、"
  "早停(EarlyStopping)、数据增强/加数据、降模型复杂度。"),
 ("bf16 和 fp16 区别，用哪个？",
  "都是半精度。bf16 指数位多、动态范围大、不易溢出，更稳(A100/H100 用它)；fp16 精度略高但易上下溢，"
  "需配 GradScaler。现代卡优先 bf16。"),
 ("Trainer 和自定义训练循环(Accelerate)怎么选？",
  "标准微调用 Trainer(封装好日志/评估/保存/多卡/混合精度，代码最少)；需要插自定义逻辑(自定义损失/RL/"
  "特殊评估)用 Accelerate 自定义循环(把每步摊开，一套代码单卡/多卡/多机透明切换)。"),
],
"三、分词器(Ch6)": [
 ("BPE / WordPiece / Unigram 区别？各家用哪个？",
  "都是子词算法。BPE 从小词表合并‘最高频’的相邻对(GPT家族)；WordPiece 合并‘得分最高(最黏)’的对(BERT)；"
  "Unigram 从大词表删‘对损失影响最小’的 token(T5/XLNet)。"),
 ("为什么用子词，不用整词或纯字符？",
  "整词→词表爆炸且生僻词全 [UNK]；纯字符→序列太长、丢语义。子词两头折中：常见词整块、生僻词拆成认识的零件。"),
 ("快速分词器(fast)除了快还有什么用？",
  "提供 offset 映射和 word_ids，能把 token 级结果对回原文字符位置——NER 贴回原词、QA 还原答案子串，全靠它。"),
 ("领域词汇(代码/医疗)被切得很碎怎么办？",
  "在领域语料上重训分词器(train_new_from_iterator 或 tokenizers 库)，让高频术语整块成 token，"
  "序列更短=推理更省(实测代码分词器比通用 gpt2 序列缩短约 40%)。"),
],
"四、RAG / 检索(Ch5,6)": [
 ("什么是 RAG？为什么需要它？",
  "检索增强生成：先从知识库按‘意思’检索相关文档，再拼进提示喂 LLM 生成回答。解决 LLM 不知道私有/最新知识、"
  "会幻觉的问题，且能引用来源、免重新训练模型。"),
 ("语义检索为什么用向量+余弦，不用关键词？",
  "关键词只能字面匹配，换个说法就搜不到；把文本嵌成向量后，语义相近的向量也相近，按余弦相似度检索能‘按意思找’。"
  "归一化后向量点积=余弦，只看方向(语义)不看长度(篇幅)。"),
 ("句向量为什么要 mean pooling 且用 mask 加权？",
  "模型输出每个 token 一个向量，要压成一句一个向量。直接平均会把 [PAD] 的废料向量算进去、稀释句向量；"
  "用 attention_mask 加权只平均真实 token 才准。"),
 ("生产 RAG 为什么用向量数据库，不用内存？",
  "百万~上亿向量内存放不下；向量库用 HNSW/IVF 等近似最近邻索引(ANN)毫秒级检索，还支持增量更新/元数据过滤/"
  "持久化/高可用。常用 Milvus/Qdrant/Pinecone/pgvector。"),
 ("怎么提升 RAG 效果？",
  "切块策略(200-500token+重叠)、强嵌入模型(bge-m3)、混合检索(向量+BM25关键词，RRF融合)、"
  "重排(召回top50→cross-encoder重排取top5)、元数据过滤(按权限/时间)、防幻觉(要求带引用)。"),
],
"五、LoRA / PEFT(Ch11)": [
 ("LoRA 原理？为什么能省这么多？",
  "冻结原权重，在每层旁插一对低秩小矩阵 A(d×r)、B(r×d)，只训这对(r 很小如8/16)。可训练参数骤降到<1%，"
  "梯度和优化器状态也只对这点参数，显存/存储大幅下降；适配器只几 MB，一个基座能挂多个适配器切任务。"),
 ("LoRA 的 r / alpha / target_modules 怎么设？",
  "r=秩(容量)，常 8/16/32，越大容量越强但省得越少；alpha=缩放，常设 2*r；target_modules 指定给哪些层加，"
  "注意力的 q_proj/v_proj 性价比高，要更强就 all-linear。"),
 ("QLoRA 是什么？和 LoRA 区别？",
  "QLoRA = 基座用 4-bit(NF4)量化加载(省一半以上显存)+ LoRA 只训低秩适配器。让 7B/13B 在单张消费级卡上微调。"
  "需要 bitsandbytes(CUDA)。"),
 ("merge_and_unload 干嘛？合并前后取舍？",
  "把 LoRA 增量并回基座权重，得到一个普通模型，推理零额外开销、可直接部署；但失去‘换适配器’的灵活性。按需二选一。"),
 ("SFT 和继续预训练/RLHF 的区别？",
  "继续预训练=喂大量领域文本让模型‘更懂领域’；SFT=用‘指令→理想回复’对教它听指令按格式答；RLHF/GRPO=用偏好/"
  "奖励进一步对齐人类偏好。通常 预训练→SFT→RL 三段。"),
],
"六、RLHF / GRPO(Ch12)": [
 ("GRPO 是什么？和 PPO 区别？",
  "Group Relative Policy Optimization：对同一提示采‘一组’答案，用‘组内相对好坏’当优势(谁比组内平均好就强化)，"
  "不需要单独的价值网络(critic)，比 PPO 简单省显存。DeepSeek-R1 用它训推理能力。"),
 ("GRPO 的优势(advantage)怎么算？",
  "advantage=(reward-组内均值)/组内标准差。只看‘同一题里谁更好’，天然消掉题目难度差异。注意：组内奖励要有方差"
  "(有对有错)才有学习信号——全对全错则优势全 0，学不动。"),
 ("为什么用‘可验证奖励’？",
  "数学/代码/事实题答案能自动验证(对/错、单测通过、能编译)，直接拿正确性当奖励，不用训奖励模型，最可靠。"
  "这是 R1 训推理的关键。还常配格式奖励等多个奖励加权。"),
 ("GRPO 生产训练的瓶颈是什么？",
  "采样(每步对每个提示生成一组补全)最慢，必须用 vLLM 加速；还要同时放策略模型+参考模型+采样，很吃显存，"
  "LoRA+多卡是标配。监控 reward(该升)/reward_std(别塌0)/KL(别跑离参考太远崩掉)。"),
],
"七、推理 / 部署(Ch1,3,9)": [
 ("vLLM 为什么快？两大核心优化？",
  "①PagedAttention：把 KV Cache 像操作系统分页一样管理，显存碎片少、利用率高 ②Continuous Batching：连续批处理，"
  "把不同请求的 token 动态拼在一起算，GPU 不空转。吞吐比原生 transformers 高一个量级。"),
 ("模型太大放不下怎么部署？",
  "量化(INT8/INT4，AWQ/GPTQ/FP8)缩小权重；张量并行(tensor-parallel 把一层切到多卡)；"
  "流水线并行；CPU/NVMe offload。vLLM/TGI/TensorRT-LLM 都支持。"),
 ("生产 LLM 服务架构怎么设计？",
  "客户端→FastAPI网关(鉴权/限流/日志/路由)→vLLM服务(OpenAI兼容)→GPU。网关和推理引擎解耦；服务无状态、"
  "K8s多副本+HPA按GPU利用率自动扩缩；相同prompt结果缓存(Redis)；监控 p99延迟/QPS/GPU利用率。"),
 ("训练和推理为什么要分离？",
  "职责/资源不同：训练产出模型→存模型仓库(HF Hub/MLflow/S3)→推理服务从仓库拉取加载。别在一个进程又训又服务，"
  "否则没法独立伸缩、更新、回滚。"),
 ("采样参数 temperature / top_p / top_k 各是什么？",
  "temperature 调概率分布陡峭度(高=更随机有创意，低=更确定)；top_k 只从概率最高的 k 个里采；"
  "top_p(核采样)从累积概率达 p 的最小集合里采。生成任务常 temp=0.7、top_p=0.9;确定性任务 temp=0 贪心。"),
],
"八、综合 / 系统设计": [
 ("给你一个业务(如客服)，怎么从 0 搭一个 LLM 应用？",
  "①明确任务和评估指标 ②数据(收集/清洗/标注) ③方案选型：能用现成模型+提示/RAG 就别微调；需要格式/领域控制才"
  "SFT+LoRA ④检索(RAG)接私有知识 ⑤评估(离线指标+人工) ⑥部署(vLLM+网关+监控)⑦上线灰度+持续迭代。"),
 ("什么时候该微调，什么时候用 RAG/提示词？",
  "要‘新知识/私有知识’→RAG(知识会变，微调追不上)；要‘新能力/固定格式/领域风格’→SFT微调；很多场景 "
  "好的提示词+RAG 就够了，微调是最后手段(贵、易过拟合、知识易过时)。常组合：微调管‘怎么答’+RAG 管‘答什么’。"),
 ("怎么评估一个 LLM 系统好不好？",
  "分任务：分类看准确率/F1；生成看 BLEU/ROUGE + 人工;检索看召回率/命中率;端到端看是否有据(防幻觉)、"
  "延迟 p99、成本、用户满意度。线上 A/B + 灰度。"),
 ("怎么降低推理成本？",
  "小模型/蒸馏、量化、缓存(相同请求)、批处理(vLLM连续批处理)、把简单请求路由到小模型(模型路由)、"
  "限流、KV cache 复用(prefix caching)。"),
],
}


def main(filt=None):
    for cat, items in QA.items():
        if filt and filt not in cat:
            continue
        print("\n" + "=" * 72 + f"\n {cat}\n" + "=" * 72)
        for i, (q, a) in enumerate(items, 1):
            print(f"\n  Q{i}. {q}")
            print(f"     A: {a}")
    print("\n" + "─" * 72)
    print(" 用法：python3 面试高频题库.py 微调   # 只看含‘微调’的分类")
    print(" 建议：每题先自己讲一遍(录音/写下)，再对答案；能讲清‘为什么’比背名词强。")


if __name__ == "__main__":
    main(sys.argv[1] if len(sys.argv) > 1 else None)
