"""
================================================================================
 实战练习 · 1-12 章综合整合（完整可跑参考，供你照着手写练熟）
================================================================================
 这套练习把前面各章的技术“再次手写、整合到一起”，每个文件都是**完整可运行**的参考，
 你照着亲手抄一遍/默写一遍来练熟。每处关键代码都写了「为什么用这个技术」。

 —— 几个中型案例（每个整合几章，可单独跑）——
   案例1_文本分类端到端.py     整合 Ch1(分词/模型) + Ch2(数据管道/训练) + Ch6(快速分词器)
   案例2_RAG语义检索问答.py     整合 Ch5(语义搜索/向量检索) + Ch6(嵌入/池化)
   案例3_LoRA指令微调.py        整合 Ch11(SFT/LoRA) + Ch7(训练工程)
   案例4_GRPO强化学习.py        整合 Ch12(GRPO/可验证奖励) + Ch11(LoRA)

 —— 一个大 demo（把多章串成一个完整产品流程）——
   综合大demo_智能客服全流程.py  串 Ch1情感 + Ch5检索 + Ch11微调 + Ch9上线(+ Ch6分词器贯穿)

 每章技术“为什么用”速查（练的时候对照记）：
   Ch1  pipeline/分词/softmax   —— 把文字变数字、把 logits 变概率，一切的地基
   Ch2  Trainer/动态填充/评估    —— 高效微调的标准流程；动态填充省显存
   Ch5  嵌入+向量检索(FAISS)     —— 按“意思”找，不按关键词；RAG 的检索前半段
   Ch6  快速分词器/offset        —— 切词决定序列长度(成本)；offset 把结果对回原文
   Ch7  训练工程(Accelerate等)   —— 调度/累积/裁剪/混合精度，把模型真正训起来
   Ch9  Gradio 上线              —— 把模型包成能点的网页/API，交付给人用
   Ch11 LoRA/SFT                —— 只训 <1% 参数微调大模型，消费级硬件也扛得住
   Ch12 GRPO 强化学习           —— 用“答对才给分”教模型推理(DeepSeek-R1 那套)

 运行任意一个：python3 案例1_文本分类端到端.py
================================================================================
"""
print(__doc__)
